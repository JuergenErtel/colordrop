{
  "name": "colordrop",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test",
    "build:www": "node tools/build-www.mjs",
    "cap:sync": "npm run build:www && cap sync ios && node -e \"const f='ios/App/App/capacitor.config.json',j=JSON.parse(require('fs').readFileSync(f,'utf8'));if(!j.packageClassList.includes('PurchasesPlugin'))j.packageClassList.push('PurchasesPlugin');require('fs').writeFileSync(f,JSON.stringify(j,null,'\\t'));\"",
    "cap:open": "cap open ios"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.4.1"
  },
  "dependencies": {
    "@capacitor/app": "^8.1.0",
    "@capacitor/core": "^8.4.1",
    "@capacitor/haptics": "^8.0.2",
    "@capacitor/ios": "^8.4.1",
    "@capacitor/splash-screen": "^8.0.1",
    "@capacitor/status-bar": "^8.0.2"
  }
}
